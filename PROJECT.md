# 作品规划：yt-dlp Raycast Download Console

## 一句话定位

一个把 yt-dlp 变成可持续管理的 macOS 下载控制台：从 Raycast 发起任务，在后台继续下载，在一个列表里看进度、失败原因、重试和完成后的文件。

## 它能做什么

1. 从一个 Raycast 表单下载一个或多个 URL。
2. 支持 MP4、最佳可用视频格式和 MP3 音频提取。
3. 支持按需使用本机浏览器 Cookies 处理需要登录的站点。
4. 关闭 Raycast 后继续运行，并把任务状态保存到本机。
5. 统一显示排队、解析、下载进度、合并/转码、成功、失败、取消和中断。
6. 对链接、环境、网络、限流、登录、存储和 ffmpeg 失败做分类，给出下一步建议。
7. 对失败、取消和中断任务一键重试，并保留尝试记录。
8. 下载完成后，在下一次唤起 Raycast 时给出醒目提示；任务列表置顶刚完成的任务。
9. 从任务列表直接打开文件、打开所在目录、在 Finder 中显示或复制路径。

## 它不做什么

- 不上传 URL、Cookies、任务记录或下载内容。
- 不绕过登录、付费墙、验证码或来源网站的访问控制。
- 不保证每个站点、每条内容或每个地区都能下载。
- 不替代 yt-dlp 和 ffmpeg；本机需要先安装它们。

## 目标用户

需要保存自己有权处理的公开或已登录可见媒体，同时又不想维护终端窗口、后台进程和失败日志的人。第一目标用户是 Nar 自己；公开作品则让其他 macOS + Raycast 用户可以复用这套控制面设计。

## 核心设计判断

命令行工具的问题通常不是“不会调用”，而是调用后失去控制：不知道任务是否还活着、不知道同时下载是否过载、不知道失败原因，也不知道完成文件在哪里。因此作品的核心价值是任务控制平面，而不是另一个 yt-dlp 参数表单。

## 当前版本：MVP

- 单一下载入口：`Download with Yt-Dlp`
- 任务入口：`Download Tasks`
- 独立 worker、持久化状态和最多 3 个并发下载
- 有限网络重试、环境预检、失败分类和手动重试
- 完成项置顶、文件/目录打开快捷键

## 后续路线

### V0.2：更低摩擦

- 记录上次选择的输出目录和模式，但不把 Cookies 写入任务之外的持久化配置。
- 提供任务搜索、按状态筛选和清理旧历史。
- 处理多个输出文件的任务（例如字幕、缩略图、主视频）并显示文件清单。

### V0.3：更强的可恢复性

- 任务级暂停/继续，而不只是取消/重试。
- 更细的站点诊断和可复制的匿名错误报告。
- 可配置并发上限和每站点限速策略。

### 暂不做

- 云端任务同步
- 账号系统
- 公开媒体镜像或批量爬取服务
- 自动上传或自动发布到第三方平台

## 验收标准

- 关闭 Raycast 后，活动任务仍能在 `Download Tasks` 中回读。
- 成功任务有明确完成记录，并能打开文件或所在目录。
- 失败任务不会无限停留在“下载中”，且能看到分类、建议和日志。
- 重试创建新的尝试，不覆盖原失败证据。
- 公开仓库不包含本机绝对路径、任务状态、Cookies、Token 或媒体文件。
- `npm run build` 和 worker 语法检查通过。
